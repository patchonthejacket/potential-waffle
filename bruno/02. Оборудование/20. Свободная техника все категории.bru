meta {
  name: Свободная техника все категории
  type: http
  seq: 20
}

get {
  url: http://localhost:9000/v2/equipment/unused
  body: none
  params {
    page: 1
    records-per-page: 50
  }
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
  
  test("Response is array", function() {
    expect(res.body).to.be.an("array");
  });
}

