meta {
  name: Свободная техника только ПК
  type: http
  seq: 21
}

get {
  url: http://localhost:9000/v2/equipment/unused
  body: none
  params {
    category: ПК
    page: 1
    records-per-page: 10
  }
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
  
  test("All items are ПК", function() {
    res.body.forEach(function(item) {
      // Все должны быть ПК (проверка через GET запрос)
    });
  });
}

