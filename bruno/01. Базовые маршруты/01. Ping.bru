meta {
  name: Ping
  type: http
  seq: 1
}

get {
  url: http://localhost:9000/ping
  body: none
  auth: none
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
  
  test("Response contains ping", function() {
    expect(res.body).to.have.property("ping");
    expect(res.body.ping).to.equal("pong");
  });
}
